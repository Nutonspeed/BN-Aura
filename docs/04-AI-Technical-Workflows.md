# 🧠 BN-Aura: AI & Technical Workflows

## 1. AI Skin Analysis Pipeline
กระบวนการวิเคราะห์ผิวของ BN-Aura แบ่งเป็น 4 ขั้นตอน:
1. **Quality Check**: ตรวจสอบความสว่าง (Lighting), ความชัด (Blur), และองศาใบหน้า (Angle) โดยใช้ MediaPipe
2. **Feature Extraction**: ดึงจุดเด่นของใบหน้า 468 จุด เพื่อระบุตำแหน่งหน้าผาก, แก้ม, จมูก, คาง
3. **Detection Engine**: ส่งรูปภาพไปยัง Cloud Vision API และ Custom ML Models เพื่อหา:
   - สิว (Acne) และ รอยแดง (Redness)
   - ริ้วรอย (Wrinkles)
   - รูขุมขน (Pores)
   - ฝ้า กระ (Pigmentation)
### Layer 3: Cognitive Sales Reasoning (Gemini 2.5 Pro)
- **Model**: `google/gemini-2.5-pro` (via Vercel AI Gateway)
- **Goal**: ประมวลผลจาก Layer 1 & 2 เพื่อ "ปิดการขาย"
  - **Urgency Scoring**: วิเคราะห์ความจำเป็นเร่งด่วนในการทำหัตถการ (Hot Lead Generation)
  - **Aesthetic Sequencing**: วางแผน Roadmap ความสวยแบบ Step-by-step เพื่อให้ลูกค้าเห็นภาพความเปลี่ยนแปลง
  - **Sales Hook Generation**: สร้างคำแนะนำที่กระตุ้นการตัดสินใจ (Emotional & Logical Sales Pitch)

---

## 2. The "Ritualistic Scan" Experience (ความขลัง)

เพื่อให้ BN-Aura ดูพรีเมียมและน่าเชื่อถือเทียบเท่าเครื่องวิเคราะห์ผิวราคาหลักล้าน UI จะถูกออกแบบให้มีลำดับขั้นตอนดังนี้:
1. **Calibration Phase**: แอนิเมชันตรวจจับใบหน้าแบบ Matrix-style พร้อมเสียงประกอบที่ดูล้ำสมัย
2. **Deep Tissue Scanning**: แอนิเมชัน "Scan Line" เลื่อนผ่านใบหน้าช้าๆ พร้อมแสดงผลลัพธ์ย่อย (Sub-analysis) แบบเรียลไทม์
3. **Neural Synthesis**: แสดงการเชื่อมโยงข้อมูล (Data Constellation) ระหว่างพิกัดใบหน้ากับฐานข้อมูลทางการแพทย์
4. **The Reveal**: แสดงผลลัพธ์ "Aura Genome" ในรูปแบบ 3D Visualization ที่ดูเป็นวิทยาศาสตร์สูงสุด

---

## 3. Vercel AI Gateway Orchestration

ระบบควบคุมการเรียกใช้งาน AI เพื่อความเสถียรและคุ้มค่า:
- **Model Selection Strategy**:
  - **Clinical Analysis**: ใช้ `google/gemini-2.5-pro` เพื่อความแม่นยำสูงสุด
  - **AI Chat & Quick Scan**: ใช้ `google/gemini-2.0-flash` เพื่อความเร็วและประหยัดต้นทุน
- **Semantic Caching**: จดจำผลลัพธ์การวิเคราะห์ที่คล้ายกันเพื่อลด Latency และลดการเรียก Gemini ซ้ำซ้อน

## 2. 3D AR Simulator Workflow
- **Face Tracking**: ใช้ MediaPipe Face Landmesh ในการติดตามใบหน้าแบบ Real-time (60 FPS target)
- **Augmentation**: ใช้ Three.js (React Three Fiber) ในการซ้อนทับ (Overlay) ผลลัพธ์การทำหัตถการ:
   - Filler Simulator: จำลองความตึงกระชับ (Morph Target/Vertex manipulation)
   - Botox Simulator: จำลองการลดเลือนริ้วรอย (Texture filtering/Shader based)
   - Skin Glow: จำลองความฉ่ำวาวของผิว (PBR Materials)

## 4. Sales Lead to Booking Flow
1. **Lead Capture**: เก็บข้อมูลจากการสแกนผิวหรือฟอร์มหน้าเว็บ
2. **AI Recommendation (Smart Mapping)**: 
   - AI วิเคราะห์ปัญหาผิวและแนะนำ "ประเภทหัตถการมาตรฐาน" (เช่น Pico Laser)
   - ระบบทำการ Query หาคอร์สจริงในคลินิกที่ตรงกับประเภทนั้นๆ (Mapping via `treatments` table filtered by `clinic_id`)
   - เซลล์ได้รับคำแนะนำที่พร้อมขายทันที พร้อมราคาจริงของคลินิกนั้นๆ
3. **Digital Proposal**: สร้างหน้าเว็บข้อเสนอพิเศษที่มี Interactive 3D/AR ให้ลูกค้าดู
4. **Real-time Tracking**: ระบบแจ้งเตือนเซลล์เมื่อลูกค้าเปิดดูข้อเสนอ
5. **Conversion**: ลูกค้ากดจองคิว (Appointment) ผ่านหน้าข้อเสนอ

## 4. Multi-tenant Quota System
- ระบบนับจำนวนการสแกนผิว (Scan Quota) ของแต่ละคลินิก
- มีระบบ Alert เมื่อ Quota ใกล้หมด
- ระบบจำกัดจำนวน Sales Staff ตาม Subscription Plan
