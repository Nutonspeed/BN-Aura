# 🧪 BN-Aura: Testing & QA Strategy

เพื่อให้มั่นใจว่าระบบ BN-Aura ที่สร้างขึ้นใหม่ทำงานได้ถูกต้อง 100% และไม่มีข้อผิดพลาดเรื่องความปลอดภัย (RLS) ระบบ BN-Aura ควรมีขั้นตอนการทดสอบดังนี้:

## 1. Unit Testing (Logic Verification)
เน้นทดสอบฟังก์ชันการคำนวณและ Logic ภายใน:
- **AI Mapper**: ทดสอบการแปลงผลลัพธ์จาก AI มาเป็น Schema ของระบบ
- **Auth Utils**: ทดสอบการจัดการ Role และ Permission
- **Scoring Engine**: ทดสอบน้ำหนักการคำนวณคะแนนผิว (Weighted Average)

```bash
# รัน Unit Tests ทั้งหมด
pnpm test:unit
```

## 2. E2E Testing (Customer Journey)
ใช้ Playwright ในการทดสอบประสบการณ์ผู้ใช้จริง:
- **Smoke Test**: หน้าแรกโหลดสำเร็จและเปลี่ยนภาษาได้
- **Auth Flow**: การสมัครสมาชิก, การรับคำเชิญเข้าคลินิก, และการล็อกอินแยกตาม Role
- **Analysis Flow**: อัปโหลดรูปภาพ -> รอผลวิเคราะห์ -> ตรวจสอบผลลัพธ์บน UI
- **Booking Flow**: การจองคิวและการตรวจสอบตารางเวลา

```bash
# รัน E2E Tests ด้วย Playwright
pnpm test:e2e
```

## 3. Database Security Audit (RLS Testing)
ส่วนที่สำคัญที่สุดของระบบ Multi-tenant:
- **Tenant Isolation**: ตรวจสอบว่าพนักงานคลินิก A ไม่สามารถมองเห็น Leads ของคลินิก B
- **Role RBAC**: ตรวจสอบว่า Sales Staff ไม่สามารถเข้าหน้า Super Admin ได้
- **Function Hardening**: ทดสอบ RPC Functions ว่าต้องมีการส่ง Auth UID ที่ถูกต้องเสมอ

```bash
# รันสคริปต์ตรวจสอบความปลอดภัยฐานข้อมูล
pnpm test:sales:smoke
```

## 4. Performance & Build Verification
- **Type-Check**: ต้องผ่าน 100% โดยไม่มี `any` ที่ไม่จำเป็น
- **Lint**: ปฏิบัติตามมาตรฐาน ESLint (React 19 rules)
- **Build Speed**: ตรวจสอบว่า `FAST_BUILD=1` ทำงานได้จริงบน Vercel

```bash
# ตรวจสอบความถูกต้องของ Code
pnpm type-check
pnpm lint
```

## 5. UI/UX Aesthetic Checklist
ก่อนส่งมอบงาน (Handover) ต้องตรวจสอบ:
- **Animations**: ต้องลื่นไหล (60 FPS) ไม่กระตุก
- **Responsive**: ตรวจสอบหน้าจอ iPhone 15, iPad Pro, และ Desktop 4K
- **Thai Typography**: สระไม่ลอย, เว้นวรรคถูกต้อง, และไม่ใช้ Font หนักจนอ่านยาก
- **Glassmorphism**: พื้นหลังเบลอต้องทำงานได้ในทุก Browser หลัก (Chrome, Safari, Firefox)
