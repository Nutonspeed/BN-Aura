# 💰 BN-Aura: Quota & Billing Specification

เพื่อให้ระบบ BN-Aura มีความยั่งยืนทางการค้าและรองรับการขยายตัว (Scalability) ระบบโควตาจะถูกออกแบบดังนี้:

## 1. Quota Structure

ระบบจะแบ่งโควตาออกเป็น 2 ประเภทหลัก:

### A. Subscription Quota (Monthly)
- มาพร้อมกับแพ็กเกจรายเดือน (เช่น Starter: 100 scans, Pro: 500 scans)
- รีเซ็ตทุกวันตัดรอบบิล (Billing Cycle)
- หากใช้ไม่หมด **จะไม่ทบไปยังเดือนถัดไป**

### B. Add-on Quota (Top-up)
- ซื้อเพิ่มได้เมื่อโควตาหลักหมด
- **ไม่มีวันหมดอายุ** (ใช้จนกว่าจะหมดจำนวน)
- ระบบจะหักโควตาจาก Subscription Quota ก่อนเสมอ หากหมดจึงจะมาหักจาก Add-on

## 2. Implementation Logic

### Database Schema Changes
ตาราง `clinics` จะถูกเพิ่มคอลัมน์ดังนี้:
- `monthly_scan_quota`: จำนวนสแกนคงเหลือในรอบเดือน
- `addon_scan_quota`: จำนวนสแกนคงเหลือจากการซื้อเพิ่ม
- `next_reset_date`: วันที่ระบบจะรีเซ็ตโควตารายเดือน

### Usage Tracking
- ทุกครั้งที่มีการเรียกใช้ AI ในระดับ `gemini-2.5-pro` (Clinical Analysis) ระบบจะทำการหักโควตา 1 หน่วย
- การสแกนแบบ `gemini-2.0-flash` (Quick Scan) อาจคิดโควตา 0.5 หน่วย หรือตามตกลง

## 3. Quota Enforcement (Middleware/Edge)
- ก่อนการเรียกใช้ AI API ทุกครั้ง ระบบจะตรวจสอบโควตาผ่าน `get_clinic_quota()`
- หากโควตาหมด ระบบจะส่งคืน Error Code `403_QUOTA_EXCEEDED`
- UI จะแสดงปุ่ม "Buy More Scans" ทันทีเพื่อให้คลินิกสามารถทำธุรกรรมต่อได้โดยไม่เสียโอกาสการขาย

## 4. Sales Staff Experience
- เซลล์สามารถดูโควตาสแกนที่เหลือของทีมได้จากแดชบอร์ด
- มีระบบแจ้งเตือนเมื่อโควตาเหลือต่ำกว่า 10% เพื่อให้เจ้าของคลินิกเติมเงินทันที
