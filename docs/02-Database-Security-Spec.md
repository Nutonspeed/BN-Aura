# 🔐 BN-Aura: Database & Security Specification

## 1. Supabase Architecture
โครงการใช้ Supabase เป็นศูนย์กลางของ Data และ Auth โดยเน้นการใช้ PostgreSQL Features อย่างเต็มประสิทธิภาพ ภายใต้โครงสร้างของ BN-Aura

## 2. Core Tables Structure

### 🏥 Clinic Management
- `clinics`: ข้อมูลหลักของคลินิก (ชื่อ, โลโก้, แผนการสมัครสมาชิก)
- `branches`: ข้อมูลสาขา (ตำแหน่ง, ข้อมูลติดต่อ)
- `clinic_staff`: เชื่อมโยง User เข้ากับ Clinic และ Role ประจำคลินิก (รองรับพนักงานทำงานหลายคลินิก)

### 👤 User Management
- `users`: ข้อมูลโปรไฟล์หลักของผู้ใช้ (Public/Common info)
- `user_app_roles`: เก็บ Global Role สำหรับระบบ (เช่น super_admin)

### 🧪 AI & Analysis
- `skin_analyses`: ผลการสแกนผิว, คะแนน (Score), และคำแนะนำ
- `scan_results`: ข้อมูลดิบจากการวิเคราะห์ (Raw data)
- `analysis_images`: เก็บ URL ของรูปภาพที่อัปโหลดไปที่ Storage

### 💰 Sales & CRM
- `sales_leads`: รายชื่อลูกค้าที่สนใจ (Leads) พร้อมคะแนน AI Scoring
- `sales_proposals`: ข้อเสนอที่ส่งให้ลูกค้า พร้อมรายละเอียดไอเทมและราคา
- `sales_commissions`: ระบบบันทึกและติดตามค่าคอมมิชชั่นของพนักงานขาย (Real-time)
- `customer_sales_assignments`: ข้อมูลการมอบหมายพนักงานขายให้ดูแลลูกค้าเฉพาะราย

### 💬 Communication & Intelligence
- `customer_sales_messages`: ประวัติการแชทระหว่างลูกค้าและพนักงานขายผู้ดูแล
- `notifications`: ระบบแจ้งเตือนทั่วทั้งระบบ (Real-time & Persistent)
- `customer_treatment_journeys`: ระบบติดตามไทม์ไลน์การรักษาตั้งแต่สแกนจนจบเคส

### 💉 Pricing & Services
- `clinic_treatment_pricing`: ราคาค่าบริการรายหัตถการเฉพาะของแต่ละคลินิก
- `inventory_products`: คลังสินค้าและผลิตภัณฑ์สกินแคร์พร้อมระบบพยากรณ์สต็อก

### 📅 Operations
- `appointment_slots`: ตารางนัดหมาย (Canonical object)
- `customers`: ฐานข้อมูลลูกค้าเชิงลึกของคลินิก
- `treatments`: แคตตาล็อกบริการกลางของระบบ

---
**อัปเดตล่าสุด**: 31 มกราคม 2569

## 3. Row Level Security (RLS) - "The Golden Rule"
ทุกตารางใน Schema `public` ต้องมี RLS เปิดใช้งาน (ENABLED) เสมอ โดยมีเงื่อนไขดังนี้:

### Hierarchical RLS Policies (แบบลำดับชั้น)
ระบบใช้ RLS เพื่อบังคับใช้โครงสร้างการจัดการ:

1. **Super Admin Access**:
   - `USING (auth.jwt() ->> 'role' = 'super_admin')` - เข้าถึงได้ทุกแถวทุกตาราง

2. **Clinic Isolation**:
   - `USING (clinic_id = (auth.jwt() ->> 'clinic_id')::uuid)` - แยกข้อมูลตามคลินิก

3. **Sales/Staff Access**:
   - พนักงานเห็นเฉพาะลูกค้าและรายการวิเคราะห์ภายในคลินิกตนเอง

4. **Customer Isolation**:
   - ลูกค้าเห็นเฉพาะข้อมูลที่เชื่อมโยงกับ `user_id` ของตนเอง (ReadOnly สำหรับผลวิเคราะห์)

### Authentication & Provisioning Security
- **Disable Public Signups**: ต้องตั้งค่าใน Supabase Dashboard (Auth > Settings) ให้ปิด "Allow new users to sign up"
- **Service Role Management**: ใช้ `service_role` key เฉพาะในฝั่ง Server-side (Admin Actions) เพื่อสร้าง User และข้าม RLS ในกรณีที่จำเป็นโดย Super Admin

> **Note**: ระบบใช้ Custom JWT Claims ผ่าน Supabase Hook เพื่อลดภาระการ Query ซ้ำซ้อนในระดับ Database Policy

## 4. Custom SQL Functions & RPCs
- `get_user_role()`: ดึง role ของ user ปัจจุบัน
- `get_user_clinic_id()`: ดึง clinic_id ของ user ปัจจุบัน
- `is_super_admin()`: ตรวจสอบว่าเป็น super admin หรือไม่
- `increment_proposal_view_count(proposal_id)`: อัปเดตยอดวิวข้อเสนอแบบ Atomic

## 5. Storage Buckets
- `analysis-images`: เก็บรูปสแกนหน้า (Private, RLS controlled)
- `clinic-assets`: โลโก้และรูปภาพคลินิก (Public read)
- `customer-documents`: เอกสารแนบ (Private)
